# ----------------------------------------------------------------------
# IMPORTANT: This configuration enforces a unified base URL across all
# environments. The .env.development and .env.production files do not
# override this value to ensure consistency and prevent mismatches.
# ----------------------------------------------------------------------
VITE_BASE_URL=http://host.docker.internal
# ----------------------------------------------------------------------
